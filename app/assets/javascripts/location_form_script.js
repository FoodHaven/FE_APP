document.addEventListener('DOMContentLoaded', function() {
  var latitude = 'Your JavaScript Value'; // Replace this with the JavaScript value you want to assign

  var formField = document.getElementById('latitude-field');
  if (formField) {
    formField.value = latitude;
  }

  var longitude = 'Your JavaScript Value'; // Replace this with the JavaScript value you want to assign

  var formField = document.getElementById('longitude-field');
  if (formField) {
    formField.value = longitude;
  }
});