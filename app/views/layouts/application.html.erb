<!DOCTYPE html>
<html>

  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>FoodHavenFeApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_import_module_tag "custom" %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container d-flex justify-content-between">
        <a class="navbar-brand" href="/">FoodHaven</a>
        <div>
          <% if session[:user_id] %>
            <div class="btn-group">
              <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-outline-danger me-2 btn-lg" %>
              <%= button_to "Favorites", users_favorites_path, method: :get, class: "btn btn-outline-success btn-lg" %>
            </div>
          <% else %>
            <div class="btn-group">
              <%= button_to "Register for new account", new_user_path, method: :get, class: "btn btn-outline-primary me-2" %>
              <%= button_to "Login", login_path, method: :get, class: "btn btn-outline-primary me-2" %>
              <%= button_to "Login with Google", '/auth/google_oauth2', class: "btn btn-outline-primary me-2" %>
            </div>
          <% end %>
        </div>
    </div>
  </div>
</nav>
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: name %>
    <% end %>
    <%= yield %>
  <footer>
  </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>

